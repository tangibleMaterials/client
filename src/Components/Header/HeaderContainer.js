import NavBar from './NavBar.js';
import './header.css';


const HeaderContainer = () => {
    return(
        <header>
            <NavBar />
        </header>
    )
}

export default HeaderContainer;
